# Dependencies
node_modules

# Next.js
.next/
out/

# Production
build/

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*

# Local env files
.env*.local

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Prettier
.prettierignore

# IDE
.idea
.vscode

# Sandbox artifacts (bare-repo internals that appear at repo root)
/HEAD
/config
/hooks
/objects
/refs

# Loose home-dir dotfiles bind-mounted at repo root (sandbox artifact)
/.bash_profile
/.bashrc
/.profile
/.zprofile
/.zshrc
/.gitconfig
/.gitmodules
/.mcp.json
/.ripgreprc

# Playwright MCP scratch output
.playwright-mcp/

# Claude Code: per-user local overrides (shared config is committed)
.claude/settings.local.json

# Endstate installer binaries (copied in manually before deploy; see
# public/downloads/README.md). The README itself is committed.
/public/downloads/*.exe
/public/downloads/*.msi
