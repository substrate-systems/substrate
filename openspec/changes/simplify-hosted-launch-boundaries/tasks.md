## Current execution order: owner, friends, redesign, public

This section is the implementation entry point. It refines existing tasks; it does not declare unchecked work complete. Reconcile shipped code before reopening a task. The earlier section 3 gateway work is deferred until friends acceptance. Planning delivery changes no production state.

- [x] P.1 Reproduce the empty-fleet admission deadlock in disposable PostgreSQL with v2 issuance explicitly enabled. Add `runtime-target-admission.integration.test.ts` alongside `hosted-paired-acceptance.test.ts` using ordinary import/activation/invite/OAuth operations and no seeded live candidate or bound cell; retain the old seeded tests as post-admission coverage. Confirm the 0.77.0 command-binding package and runtime gateway digest from their actual producers, and freeze the target/provenance fixture shared with Exomem. Acceptance: the new happy-path journey fails for the current circular prerequisite, while negatives prove absent/unsigned/mismatched targets remain refused.
- [x] P.2 Add an absent-or-identical authenticated runtime-target record through an additive migration and `admin/contracts`, using release/deployment proof rather than observed cells. Update `agent-contract-store.ts`, `promotion-runtime.ts`, `hosted-cohort-target.ts`, admission target CTEs in `db.ts`/`oauth-store.ts`, and payment-triggered target selection. Keep existing populated-fleet verification; implement empty-fleet activation under the shared fence and protect in-flight work. Acceptance: no-cell activation/admission, last-cell deletion/readmission, idempotent activation and concurrent activation/admission tests pass; wrong tuple/provenance and conflicting work (including a same-target canary whose assignment would be retired) refuse without partial writes.
- [ ] P.3 Audit and remove any remaining fleet/artifact dependency from ordinary client resolution, code exchange, refresh and immutable-contract discovery. Keep cell readiness on command execution. Verify `getLiveExomemAgentContract`, account install actions, `mcp.ts`, gateway and per-platform views, including actual selected-host registration/metadata. Acceptance: a configured approved uncertified host connects while an unapproved host, revoked grant, wrong audience or mismatched cell is denied; preparing does not consume a new invite or invalidate refresh.
- [ ] P.4 Expose the existing operator/customer boundaries needed by the companion launch coordinator, including read-only preflight/status and same-attempt result lookup after lost responses. Add an API only where the existing surface cannot expose the required bounded status; do not seed production records. Acceptance: email-first/OAuth-first, session expiry, duplicate invite redemption and loss after every committed effect resume the same tenant/operation and target without secrets in the manifest.
- [ ] P.5 Prove the paid-friends path with the current configured price policy: payment before provision, verified webhook replay, failed/abandoned checkout, cancellation/suspension and capacity races. Use isolated local and provider-sandbox fixtures first; do not switch production billing mode or charge a real card automatically. Acceptance: unpaid users cannot provision/use a cell; duplicate events allocate once; unavailable capacity cannot produce an unusable paid admission. Identify any real purchase requiring operator action in the friends report.
- [ ] P.6 Run affected activation/OAuth/lifecycle/MCP/PostgreSQL tests during implementation. At the phase delivery boundary run the repository's full test suite, build and strict OpenSpec validation; ensure PostgreSQL integration actually ran. Obtain independent security/correctness review of the actual diff and concurrent negatives, fix findings, push ready PRs, and merge/deploy only with authority. No repeated green full-suite run for docs-only follow-ups.
- [ ] P.7 Integrate with Exomem P.1–P.5: pass the connected local empty-installation journey and real-runtime/cluster journey, then preflight live identities and complete owner acceptance on the current gateway with one durable run. Preserve the existing owner invite; do not use reviewer bootstrap. Owner confirmation releases the friends milestone; no friend invitation is sent by the test result itself.
- [ ] P.8 After owner confirmation, complete paid-path and live isolation/recovery/capacity evidence and issue only the authorized friend invitations. Preserve recovery headroom and count the owner's slot. Later, resume section 3 gateway work and measured performance, then explicitly scope public self-service, waitlist/pricing transition, host certification and operational readiness in their existing OpenSpec changes.

P.1/P.2 completion evidence: the checked 0.77.0 manifest was reproduced from both
verified release subjects and exact producer/consumer fixtures; tampered evidence
was refused. Fresh-schema PostgreSQL acceptance uses all production migrations
and proves imported-target activation, OAuth-first admission with one capacity
reservation, retry/race safety, last-cell target persistence and conflicting
canary refusal. Independent payment and lifecycle races verify the shared fence.
These are store-level proofs: browser consent, full deletion/recovery workflow,
real provisioning, useful MCP and live owner acceptance remain P.4/P.7 work.

P.3 service checkpoint: the fresh-schema acceptance now registers Claude through
the admitted-host CIMD path using its public metadata shape, creates ordinary
authorization and token state, and exercises the real MCP handler through the
official client. Initialization, tool listing and refresh work without a cell or
certified artifact, including after a nonready cell is attached; commands remain
`CELL_PREPARING`, wrong-resource refresh is refused, and family revocation denies
access. Ordinary discovery no longer depends on the routable-cell projection.
This is an in-process protocol/database test, not Claude host consent or real
cell serving. P.3 remains open for its remaining audit/negative coverage, and the
resumable public-route and real-cluster journey remains P.4/P.7.

### Delegation contract

The design's Runtime target import contract fixes P.1's producer, verifier, schema and API. P.1 verifies/materializes that contract and reproduces the defect before dispatch of dependent work; a contradictory result returns to the primary lane. Then use one Substrate author for P.2–P.4 and one Exomem author for the coordinator/drill, each in its own worktree with no overlapping files, isolated test state and a per-task resume pointer. P.5 billing checks can remain with the Substrate author. Integrate after both satisfy their local contracts; one independent reviewer checks the paired admission/lifecycle interaction and its important negative reproductions. A final verifier owns the actual end-to-end milestone report. These roles answer different questions; do not add duplicate review layers.

Use lane-delegate Stage 0 to rederive load-bearing claims and reject unresolved producer/consumer contracts before implementation. Choose explicit models/effort from current harness capabilities: strong reasoning for the target/admission decision and independent security review, a capable lower-effort model for settled implementation, medium for routine orchestration. The plan is model-independent. A changed contract returns to the primary lane; it is not delegated as an implementation guess. Preserve the existing full-suite delivery boundary and scoped iteration checks.

## 1. Intake and compatibility contract

- [ ] 1.1 Record current activation, OAuth and private-route behavior from `oauth-store.ts`, `agent-contract-store.ts`, `hosted-cohort-target.ts`, `mcp.ts` and `gateway.ts`; verify the artifact gate with a failing approved-client/no-artifact PostgreSQL integration test and preserve an authenticated latency baseline without secrets.
- [ ] 1.2 Import the Exomem companion's canonical command-binding fixture and pin its digest in contract compatibility; verify producer-shaped fixtures agree across repositories and malformed/duplicate expected headers are covered before wiring the new route.
- [ ] 1.3 Reconcile artifact-admission wording in the active `scope-cohort-admission-per-platform`, `add-exomem-hosted-mcp-oauth` and `admit-cimd-clients-by-host` artifacts with this change while preserving their unaffected security contracts; verify no remaining current-plan service predicate requires a promoted artifact and run strict OpenSpec validation.

## 2. Runtime activation and service admission

- [ ] 2.1 Add runtime-only activation and independent artifact certification in the contract store and additive migration; red-first extend `agent-contract-postgres.integration.test.ts`, `agent-contract-routing-cas.integration.test.ts` and `platform-cohort-postgres.integration.test.ts` for no-artifact activation, pending-artifact/live-runtime certification, stale fleet, ambiguous target, lifecycle races and idempotent retry.
- [ ] 2.2 Apply the shared service-client predicate across registered/CIMD lookup, authorization start/completion, invite bootstrap, code exchange, access validation, refresh and contract lookup; verify each stage accepts approved uncertified clients and still rejects unapproved clients, bad PKCE/redirect/audience, replay and revoked/expired metadata using the OAuth unit and PostgreSQL integration tests.
- [ ] 2.3 Separate durable OAuth policy from transient cell readiness and preserve atomic provisioning target snapshots; verify cell outage permits valid refresh but blocks tools, explicit revocation/suspension denies access, discovery performs no cell request and repeated reconnect creates no lifecycle/capacity duplicates in `oauth-bootstrap-postgres.integration.test.ts`, `mcp.test.ts` and lifecycle tests.
- [ ] 2.4 Remove only obsolete artifact-cohort hot-path locking/predicates and retain token/lifecycle serialization; verify race tests under real disposable PostgreSQL and compare SQL stage counts/timings with the recorded baseline.
- [ ] 2.5 Terminalize canary stages/assignments and revoke their entire credential/transaction/code/grant/token lineage atomically on activation, removing the live-candidate escape; red-first test activation versus code exchange/refresh, idempotent cleanup, no surviving canary credential, preserved ordinary grants and successful later certification from the preserved exact artifact evidence.
- [ ] 2.6 Permit exact signed absent-or-identical platform locks and normal-service host evidence registration against the live runtime without active canary stages; verify second-platform certification after activation, retired-stage evidence provenance, wrong-digest rejection and no runtime-identity mutation in contract/canary PostgreSQL integration tests.

## 3. Canonical shared gateway

- [ ] 3.1 Add bounded immutable contract/tool-validator caching in `mcp.ts` keyed by the full identity; red-first verify reuse, changed-digest invalidation, bounded eviction and immediate revocation/suspension despite a warm cache in `mcp.test.ts`.
- [ ] 3.2 Add the capability-selected single-command private path in `gateway.ts`; red-first verify one POST/no GET, complete tuple propagation, mismatch without legacy fallback, old-runtime GET compatibility and unchanged idempotency in `gateway.test.ts`.
- [ ] 3.3 Add fixed-origin cluster-local transport with exact stored-host/mapped-path validation; verify malicious public routing headers, malformed stored endpoints, redirects and alternate-cell attempts are denied, while the existing remote transport stays HTTPS-only.
- [ ] 3.4 Add `src/exomem-gateway/` Node streaming adapter, pinned image/build entrypoint and minimal environment contract, reusing the canonical MCP handler; verify no Next request dependency, request cancellation, streaming backpressure, body limits, stateless multi-instance operation, readiness and graceful drain in adapter tests.
- [ ] 3.4a Add the explicitly dispatched main-only gateway image publisher and deployment verification runbook; require green CI at the exact source SHA, a source-bound immutable GHCR digest and verified repository/workflow provenance, with no preview publishing, automatic deployment or personal registry token.
- [ ] 3.5 Configure the exact-path native Vercel external rewrite ahead of the existing local MCP handler, targeting the companion's tunnel origin; verify built route precedence/no regional MCP invocation, explicit CDN caching disable, private/no-store success/errors, streaming/timeout/cancellation, trusted ingress source versus spoofed forwarded headers, cross-principal cache denial and unchanged website/OAuth/transfer routes.

## 4. Agent-run acceptance support and delivery

- [ ] 4.1 Provide the companion acceptance runner a normal OAuth/protocol test client and isolated browser-flow fixtures; verify fresh authorization, refresh/replay, revocation, wrong audience and cell-outage behavior through public routes without database-inserted production tokens or bypass flags.
- [ ] 4.2 Run affected tests during each round using `node --import tsx --experimental-test-module-mocks --test <exact changed test files>` and disposable PostgreSQL for integration files; at tranche completion run `npm test`, `npm run build` and `npm run openspec:validate`, retaining actual output and naming unrelated baseline failures.
- [ ] 4.3 Obtain author-independent review of the integrated diff and real race/denial reproductions before delivery; verify both the fixture contract and gateway/cell interaction against the paired Exomem build, then commit/push a ready PR with scoped evidence under repository policy.
- [ ] 4.4 Complete owner and friends admission/service acceptance on the current gateway in P.7–P.8 order. After friends acceptance, deploy the companion gateway/edge path and verify its separate transport/performance report. Keep genuine per-host certification separate and record irreducible consent once.
- [ ] 4.5 Update operator runbooks for activate-runtime, certify-client, reconnect, rollback and resource-bounded acceptance; verify documented commands against the shipped interface, synchronize canonical OpenSpec deltas without overwriting newer requirements, and archive only after all non-optional implementation and delivery evidence exists.

## Execution dependencies

One author owns admission and its database transactions (section 2). Gateway work (section 3) can proceed independently after the shared fixture in 1.2 is settled; it must not edit admission queries concurrently. The Exomem runtime binding can proceed alongside admission. Gateway integration/public cutover waits for both reviewed transport implementations. Private-alpha acceptance uses the current gateway after its phase-specific dependencies pass. Missing deployment secrets/route authority or external host consent block only their dependent live stages, not local implementation/testing. No production implementation is claimed by this planning artifact.
