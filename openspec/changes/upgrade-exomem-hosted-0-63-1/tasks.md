## 1. Exact Release Artifacts

- [x] 1.1 Add red tests for exact Exomem `0.63.1` commit/profile selection and rejection of untrusted release-profile combinations.
- [x] 1.2 Extend the contract generator for the cataloged v4 candidate directory and verify Claude, OpenAI, compatibility, and gateway command parity.
- [x] 1.3 Generate checked 0.63.1/v4 agent and full gateway fixtures from clean commit `35f6d7bb`, including exactly 25 commands.

## 2. Profile-Bound Control Plane

- [x] 2.1 Add red tests proving v4 import/admission while retained v1 fixtures remain distinct catalog units.
- [x] 2.2 Add 0.63.1/v4 to the trusted catalog and advance the current private-alpha profile without rewriting legacy rows.
- [x] 2.3 Replace current-flow v1 literals across lifecycle, OAuth, reviewer, artifact, observation, and promotion paths with the central v4 profile authority.
- [x] 2.4 Add regression tests that reject stale v1 candidate/evidence as authority for v4 operations.

## 3. Registry-Derived Gateway

- [x] 3.1 Add red tests for v4 release/profile fixture selection, dynamic private agent routes, and caller selector rejection.
- [x] 3.2 Select the gateway fixture by exact release/profile and construct private contract/command paths from the validated routed profile.
- [x] 3.3 Verify Claude and OpenAI MCP discovery expose the same ordered 25-tool v4 surface.

## 4. Verification and Operations

- [ ] 4.1 Run focused unit and Postgres-backed control-plane tests, full tests, lint, build, and strict OpenSpec validation.
- [x] 4.2 Update the hosted-alpha runbook and runtime trust report with exact 0.63.1/v4 pins and the fresh paired-review sequence.
- [ ] 4.3 Deploy the control plane, import/stage the v4 candidate, and roll a reviewer cell forward in place with continuity and identity verification.
- [ ] 4.4 Complete fresh Claude and ChatGPT smoke evidence in one window, promote once, and verify paid-alpha discovery reports all 25 tools.
